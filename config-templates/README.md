# OpenCraw Init Templates

These files are the authoritative templates used by `opencraw init`.

`opencraw init` copies them into `~/.opencraw/` if they do not already exist:

- `~/.opencraw/config.toml`
- `~/.opencraw/configs/*.toml` provider fragments

Templates are intentionally secret-free. Fill real values only in local files
under `~/.opencraw/`.
