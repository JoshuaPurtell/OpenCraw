# Gmail API Contracts

Run the generator to snapshot the Gmail Discovery contract and semantic type inventory:

```bash
scripts/fetch-gmail-contracts.sh
```

Generated artifacts in this directory are the source of truth for Gmail method/schema semantics used by OpenCraw.
